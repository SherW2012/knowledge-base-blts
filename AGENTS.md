# Knowledge Base Agent Guide

这个仓库不是代码项目，而是 Markdown 知识库。

## 总原则

1. 优先维护真实目录与 `.md` 文件，不引入数据库式元数据系统。
2. 新内容按以下一级目录归档：`本部/`、`外交部/`、`技术部/`、`前沿部/`、`商务部/`、`艺术部/`；暂时无法判断时放入 `收件箱/`。
3. 目录深度尽量控制在「部门 → 主题 → 文档」三层左右。
4. 不创建重复文档。新增前先搜索是否已有同主题文件；已有则优先补充或重构原文。
5. 文件名使用清楚、可搜索的中文主题名，不使用日期作为唯一文件名。
6. 文档以 Markdown 为主，标题层级清晰；除非确有必要，不添加复杂 front matter。

## 大文件

- 不把图片、视频、音频等大文件直接提交到 GitHub。
- 媒体文件应放在 R2 / 对象存储中，然后在 Markdown 中引用外链。
- 仓库中只保留文本、必要的小型配置和索引文件。

## 修改原则

- 小改动优先直接编辑原文件。
- 大规模重构时保持原有信息完整，不随意删减用户已经确认的观点。
- 需要移动文档时，同步修正相关 Markdown 链接。
- 内容存在明显归属冲突时，优先保留单一主文件，通过普通 Markdown 链接建立关联。

## 隐私

当前仓库如果仍为 Public，不要写入隐私信息、公司内部资料、账号信息或其他不适合公开的内容。处理这类内容前，先确认仓库已经改为 Private。
