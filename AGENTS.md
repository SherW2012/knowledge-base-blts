# Knowledge Base Agent Guide

这个仓库以 Markdown 保存知识内容，并通过静态网页统一浏览。

## 正式来源

- GitHub 仓库的 `main` 分支是知识库的唯一正式来源。
- 不再读取或修改旧的本地知识库目录；本地目录只能用于临时构建和验证，不能作为内容源或长期副本。
- 所有后续知识补充、目录调整和网页修改都应提交到本仓库。

## 总原则

1. 优先维护真实目录与 `.md` 文件，不引入数据库式元数据系统。
2. 新内容按以下一级目录归档：`本部/`、`外交部/`、`技术部/`、`前沿部/`、`商务部/`、`艺术部/`。
3. 创意、新点子、新玩法、内容灵感、产品想法和待验证方向统一归 `前沿部/创意与新点子/`。
4. 不设置“收件箱”作为兜底目录。若内容归属确实无法判断，先根据语义判断；仍不明确时询问用户后再写入。
5. 目录深度尽量控制在「部门 → 主题 → 文档」三层左右；有明确必要时可以再增加一层。
6. 不创建重复文档。新增前先搜索是否已有同主题文件；已有则优先补充或重构原文。
7. 文件名使用清楚、可搜索的中文主题名，不使用日期作为唯一文件名。
8. 文档以 Markdown 为主，标题层级清晰；除非确有必要，不添加复杂 front matter。
9. 修改已有观点时，以用户最后确认的版本为准，不擅自“润色”成不同含义。

## 网页与索引

- `site/` 是所有部门共用的阅读界面，避免为不同知识库复制前端代码。
- `scripts/build-site.mjs` 自动扫描真实目录并生成网页索引；新增文档时不手工编辑索引文件。
- `dist/` 是临时构建产物，不直接编辑，也不提交到仓库。
- 修改网页或内容结构后运行 `npm run check`。
- 仓库包含私人内容；在没有访问控制前，不添加会自动向公网发布内容的工作流。

## 链接

- 同一仓库内部优先使用相对 Markdown 链接。
- 移动文件时同步修正受影响的相对链接。
- 一个主题只保留一个主文件；需要跨主题关联时使用普通链接，不复制全文。

## 大文件与图表

- 不把照片、视频、音频等大文件直接提交到 GitHub。
- 媒体文件应放在 R2 / 对象存储中，然后在 Markdown 中引用外链。
- 与正文紧密关联、体积较小的 SVG 可以随文档保存；SVG 应保持文字清晰、配色一致和移动端可读。
- 图表只在能显著说明结构、流程、映射或状态变化时使用。

## 修改原则

- 小改动优先直接编辑原文件。
- 大规模重构时保持原有信息完整，不随意删减用户已经确认的观点。
- 内容存在明显归属冲突时，优先保留单一主文件，通过普通 Markdown 链接建立关联。
- 新建知识前先判断：它应该成为新文件，还是已有文件中的新增章节。
- 文档面向最终读者，不写入对话过程、答题权限、设计过程或内部工作说明。

## 隐私与安全

仓库当前为 **Private**，可以保存个人知识与普通私人笔记；但仍禁止提交密码、API Token、SSH 私钥、Cookie、身份证件号码等高敏感凭据。涉及公司内部信息时，除非用户明确确认可以保存，否则不要写入仓库。
