name: Update AI intelligence radar

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy radar disabled
        run: |
          echo "Legacy automated radar is disabled."
          echo "Current rule: when the user asks for X 情报雷达 / 雷达, ChatGPT must use its own web research capabilities and directly update the 前沿部 radar data in the repository."
          echo "Do not use DragonCode or any external LLM API for radar generation."
