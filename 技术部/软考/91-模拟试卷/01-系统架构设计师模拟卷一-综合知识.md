# 系统架构设计师模拟卷（一）：综合知识

本卷按首次出题时的实际内容归档：共75道单项选择题，仅包含综合知识部分。每题1分，每题仅有一个最合适答案。本文件不附答案。

### 1. 某程序动态执行指令数为6×10⁸，平均CPI为1.5，CPU主频为3 GHz。忽略I/O等待，其CPU执行时间为

A. 0.20 s  
B. 0.30 s  
C. 1.20 s  
D. 3.00 s  

### 2. 程序原执行时间的80%可并行化，使用4个处理器，忽略并行开销。按Amdahl定律，整体加速比为

A. 2.5  
B. 4  
C. 3.2  
D. 5  

### 3. 访问Cache耗时2 ns，命中率95%；未命中时在已付出的2 ns之外，额外花费80 ns完成主存访问和填充。平均访问时间为

A. 4 ns  
B. 5.9 ns  
C. 82 ns  
D. 6 ns  

### 4. 一个单发射理想流水线有5级，每级耗时2 ns；无停顿、无冲突且流水线初始为空。连续完成20条指令需要

A. 40 ns  
B. 50 ns  
C. 48 ns  
D. 200 ns  

### 5. 6块等容量4 TB磁盘组成RAID 6，忽略格式化和元数据开销。可用容量及保证容忍的任意磁盘故障数分别为

A. 20 TB，1块  
B. 16 TB，2块  
C. 24 TB，2块  
D. 12 TB，3块  

### 6. 某向量指令对多个不同像素同时执行相同乘法。按Flynn分类，其主要体现

A. SISD  
B. MISD  
C. MIMD  
D. SIMD  

### 7. 某系统按字节编址，虚拟地址32位，页大小4 KiB，采用一级页表，每个页表项4字节。单进程完整页表的大小为

A. 4 MiB  
B. 1 MiB  
C. 8 MiB  
D. 16 MiB  

### 8. 进程有3个初始为空的页框，页面引用序列为1、2、3、1、4、2、5，采用LRU置换。缺页次数为

A. 4  
B. 5  
C. 6  
D. 7  

### 9. 运行态进程请求磁盘I/O后等待结果；I/O完成时该进程通常依次经历

A. 运行→就绪→阻塞  
B. 运行→阻塞→就绪  
C. 运行→阻塞→运行，且不经过调度  
D. 就绪→运行→终止  

### 10. 容量为N的有界缓冲区，初始empty=N、full=0、mutex=1。生产者把入队前操作写为P(mutex); P(empty)。其主要问题是

A. 必然导致两个生产者同时进入临界区  
B. empty应该初始为0  
C. P操作本身不能用于进程同步  
D. 缓冲区满时，生产者可能持有mutex等待空位，使消费者无法出队  

### 11. 系统有A、B两类资源，Available=(1,1)。P1的Allocation=(1,0)、Need=(1,1)；P2的Allocation=(0,1)、Need=(2,0)。当前状态

A. 安全，可先P1后P2完成  
B. 不安全，因为P2当前不能直接完成  
C. 已经死锁，因所有Need非零  
D. 安全，且只能先P2后P1完成  

### 12. 某索引文件有10个直接地址和1个一级间接地址。块大小4 KiB，块地址4字节，不考虑其他限制。可寻址的最大文件数据量为

A. 4096 KiB  
B. 4100 KiB  
C. 4136 KiB  
D. 40 MiB  

### 13. 多个进程的工作集总量明显超过可用页框，CPU利用率下降且换页I/O持续升高。优先采取的措施是

A. 继续增加并发进程以填满CPU  
B. 降低多道程序度，为活跃进程提供足够页框  
C. 缩短所有时间片即可消除缺页  
D. 关闭全部页面访问位维护  

### 14. 主机192.168.10.20/24需要向192.168.20.30发送IPv4分组，默认网关为192.168.10.1。在普通以太网、无代理ARP且ARP缓存为空时，首先应解析

A. 远端主机192.168.20.30的MAC地址  
B. 本机的公网IP地址  
C. DNS服务器的域名  
D. 默认网关192.168.10.1的MAC地址  

### 15. 一个IPv4网络为10.20.30.64/27，不考虑特殊地址用途，其传统可分配主机地址数量及广播地址是

A. 32，10.20.30.95  
B. 30，10.20.30.127  
C. 30，10.20.30.95  
D. 62，10.20.30.127  

### 16. TCP接收窗口较小导致发送端降低发送量；另一连接因网络拥塞触发拥塞窗口收缩。两者分别主要属于

A. 流量控制、拥塞控制  
B. 拥塞控制、流量控制  
C. 差错检测、流量控制  
D. 路由选择、差错控制  

### 17. 客户端发送SYN，初始序号为100；服务端发送SYN+ACK，初始序号为500，不携带应用数据。客户端第三次握手的SEQ和ACK通常为

A. 100和500  
B. 101和500  
C. 100和501  
D. 101和501  

### 18. 按HTTP语义，对同一URI重复发送内容相同的PUT请求，幂等性要求

A. 每次响应状态码必须相同  
B. 预期的服务器端资源效果与执行一次相同  
C. 每次响应体必须逐字相同  
D. 服务器不得记录第二次请求日志  

### 19. 浏览器访问HTTPS站点时，证书链验证成功但证书中的服务名称与访问域名不匹配。合理处理是

A. 只要数据已经加密就可忽略名称不匹配  
B. 改用服务器公钥加密全部业务数据即可解决  
C. 拒绝把该证书视为目标域名的有效身份证明  
D. DNS解析成功即可替代证书名称验证  

### 20. 系统要验证软件更新包来源并发现内容篡改，发布者应

A. 使用发布者私钥签名，接收方使用可信发布者公钥验签  
B. 用接收方公钥签名，发布者公钥验签  
C. 只附加未受保护的哈希值即可证明来源  
D. 用同一公开密码加密更新包即可证明来源  

### 21. 登录查询将用户输入拼接到SQL字符串中。针对SQL注入，最直接的代码层防护是

A. 仅把输入框长度限制为20个字符  
B. 只对传输启用TLS  
C. 将数据库端口改为非默认值  
D. 使用参数化查询，将SQL结构与输入数据分离  

### 22. 在零信任架构中，某设备接入内部网络后，访问核心资源的策略应当是

A. 只要在内部网段就默认拥有全部读取权限  
B. 依据身份、设备状态、资源及上下文进行认证授权，不仅凭网络位置授信  
C. 关闭访问日志以降低验证开销  
D. 把所有业务改成匿名访问以减少密码风险  

### 23. 数据库调整文件组织方式和索引后，逻辑表结构及应用访问接口保持不变，主要体现

A. 物理数据独立性  
B. 逻辑数据独立性  
C. 事务隔离性  
D. 分片透明性  

### 24. 关系R(A,B,C)，函数依赖集F={A→B，B→C}，属性均为原子值。R的候选键和最高满足的范式为

A. A，第三范式  
B. AB，第二范式  
C. A，第二范式  
D. B，BCNF  

### 25. R(A,B,C)满足A→B、B→C，分解为R1(A,B)和R2(B,C)。该分解

A. 无损连接但不保持依赖  
B. 无损连接且保持依赖  
C. 有损连接但保持依赖  
D. 有损连接且不保持依赖  

### 26. 查询从未下过订单的客户，Customer(id,name)、Orders(id,customer_id)中Orders.customer_id允许NULL。最稳妥的表达是

A. SELECT id FROM Customer WHERE id NOT IN (SELECT customer_id FROM Orders)  
B. SELECT c.id FROM Customer c JOIN Orders o ON c.id=o.customer_id  
C. SELECT DISTINCT customer_id FROM Orders WHERE customer_id IS NULL  
D. SELECT c.id FROM Customer c WHERE NOT EXISTS (SELECT 1 FROM Orders o WHERE o.customer_id=c.id)  

### 27. 按SQL标准隔离级别的最低保证，能够防止脏读和不可重复读、但仍允许幻读的是

A. Read Uncommitted  
B. Read Committed  
C. Repeatable Read  
D. Serializable  

### 28. 记LS(X)为申请X的共享锁，U(X)为释放X上的锁。下列哪个锁操作序列符合基本两阶段锁协议？

A. LS(A)；LS(B)；U(A)；U(B)  
B. LS(A)；U(A)；LS(B)；U(B)  
C. LS(A)；LS(B)；U(A)；LS(C)；U(B)；U(C)  
D. LS(A)；U(A)；LS(A)；U(A)  

### 29. 所有事务遵守基本两阶段锁协议，能够保证的性质是

A. 不会发生死锁  
B. 所有读操作均无需加锁  
C. 协调者失效时事务必定立即结束  
D. 产生的调度冲突可串行化  

### 30. 两阶段提交中，某参与者已持久化“准备就绪”并投赞成票，但尚未获知全局决定，此时协调者失联。在没有额外恢复协议和决定信息的经典模型中，该参与者

A. 可立即自行提交而不影响原子性  
B. 可能保持未决状态并等待全局决定  
C. 可立即自行回滚而不影响原子性  
D. 必须把两阶段锁协议切换成三阶段锁协议  

### 31. 在索引页容量相近、数据量大且实现合理的条件下，B+树相对于平衡二叉搜索树通常树高更低，主要因为

A. B+树把全部数据放在根节点  
B. B+树不需要任何比较操作  
C. 内部节点可容纳多个键和大量子指针，分支因子更高  
D. B+树允许不同叶节点任意处于不同深度  

### 32. 普通B+树复合索引为(tenant_id, created_at, status)，不考虑跳跃扫描等特殊优化。哪个查询最能直接利用其有序前缀完成定位及时间范围扫描？

A. tenant_id=7 AND created_at>=某时刻 AND created_at<另一时刻  
B. 仅status=1  
C. 仅created_at>=某时刻  
D. 仅对tenant_id执行不可索引化函数后的结果过滤  

### 33. 需求不明确，且新算法与性能目标存在较大技术风险，需要每轮先识别和消减风险，再决定后续投入。最符合这一特点的过程模型是

A. 一次性瀑布模型  
B. 螺旋模型  
C. 纯粹代码修补模型  
D. 不包含风险分析的线性实施流程  

### 34. 团队建立“需求编号—架构决策—代码模块—测试用例”的双向关联。它最直接支持

A. 自动消除全部需求变更  
B. 用测试用例替代需求确认  
C. 保证每个需求都必须单独对应一个微服务  
D. 变更影响分析与需求验证覆盖检查  

### 35. 将父DFD中的“处理订单”分解成子图时，发现子图新增外部输入“折扣审批结果”，父图中没有对应数据流。应首先

A. 检查并修正父子图边界数据流不平衡的问题  
B. 把所有数据存储改成外部实体  
C. 因为子图更详细，可以无条件增加任意外部流  
D. 把“折扣审批结果”改为控制箭头即可  

### 36. 模块A把一个“操作类型”标志传给模块B，由B依据标志选择互不相关的处理分支。两模块主要表现为

A. 数据耦合  
B. 内容耦合  
C. 控制耦合  
D. 无耦合  

### 37. 某控制流图为单一连通程序，节点数N=9，边数E=12，使用V(G)=E−N+2。其环形复杂度为

A. 3  
B. 4  
C. 6  
D. 5  

### 38. 判定为A OR B，假定不发生短路，每次均评价两个原子条件。测试集为(A=真,B=假)和(A=假,B=真)。该测试集

A. 达到判定覆盖，未达到条件覆盖  
B. 达到条件覆盖，未达到判定覆盖  
C. 同时达到判定覆盖和条件组合覆盖  
D. 既未达到条件覆盖，也未达到判定覆盖  

### 39. 按ISO/IEC 25010:2011产品质量模型，“可用性、容错性、可恢复性”属于哪个一级特性的子特性？

A. 可靠性  
B. 性能效率  
C. 兼容性  
D. 可移植性  

### 40. 系统为适配新的操作系统版本而修改文件访问代码，业务功能保持一致。该维护主要属于

A. 改正性维护  
B. 完善性维护  
C. 适应性维护  
D. 预防性维护  

### 41. 一份架构文档需要描述部署节点、运行环境以及软件制品到节点的映射，应优先使用UML

A. 用例图  
B. 状态机图  
C. 活动图  
D. 部署图  

### 42. 图像处理模块允许调用者在多种重采样算法之间切换，算法实现同一接口且可独立增加。最直接适用

A. 单例模式  
B. 策略模式  
C. 备忘录模式  
D. 享元模式  

### 43. 客户端只理解统一图像读取接口，现有第三方库的方法名和参数形式均不兼容，希望复用第三方实现。适宜采用

A. 适配器模式  
B. 观察者模式  
C. 解释器模式  
D. 抽象工厂模式  

### 44. 对影像执行勾画、删除和移动操作，需要把操作对象入队，并支持撤销。最合适的模式是

A. 原型模式  
B. 外观模式  
C. 命令模式  
D. 享元模式  

### 45. 在4+1视图中，线程、进程、通信和运行时同步主要属于

A. 开发视图  
B. 逻辑视图  
C. 物理视图  
D. 进程视图  

### 46. 系统依次完成日志解析、脱敏、转换和聚合，各阶段通过统一数据流连接，阶段可独立替换。最符合

A. 黑板风格  
B. 管道—过滤器风格  
C. 主程序—子程序风格  
D. 以共享全局状态为中心的对象组织  

### 47. 工具平台核心负责扩展点、插件生命周期和基础通信，各业务能力以插件接入，新增插件尽量不修改核心。其主导风格为

A. 微内核架构  
B. 批处理风格  
C. 纯客户端—服务器两层风格  
D. 黑板架构  

### 48. 下列哪项最可能是架构重要需求（ASR），需要在架构阶段优先处理？

A. 帮助页中某段文字由黑色改成深灰  
B. 某按钮提示文本增加一个标点  
C. 跨机房故障后核心写服务必须在60秒内恢复，且不能丢失已确认提交的数据  
D. 测试环境示例用户的显示名调整  

### 49. 关于ADD与ATAM的分工，正确的是

A. ADD专门计算项目净现值；ATAM负责生成全部源代码  
B. ADD与ATAM都是仅在上线后开展的压力测试  
C. ATAM能在没有任何架构描述的情况下完成对具体方案的完整评估  
D. ADD用于由架构驱动因素引导设计分解；ATAM用于分析风险和质量属性权衡  

### 50. 订单上下文中的“客户”强调下单与信用额度，客服上下文中的“客户”强调联系记录与工单。按DDD，较合理的处理是

A. 强制所有上下文共享同一张超大客户表与同一个对象类  
B. 在各限界上下文内保持模型与语言一致，跨边界显式转换  
C. 每个数据库表必然单独形成一个限界上下文  
D. 一旦存在同名概念就禁止服务间通信  

### 51. 团队从多个同类产品中提炼领域共性和可变点，建立参考架构供后续产品裁剪。这最符合

A. DSSA与领域工程的思路  
B. 针对单次缺陷的补丁维护  
C. 逐行代码压缩  
D. 仅针对一次业务查询的SQL调优  

### 52. 场景“业务高峰期，医生发起影像查询，影像服务返回结果，95%的请求在2秒内完成”中的“95%的请求在2秒内完成”是

A. 刺激源  
B. 制品  
C. 响应度量  
D. 环境  

### 53. 建立ATAM效用树时，对叶节点场景常用的两个优先级维度是

A. 代码行数与变量名称长度  
B. 文档页数与图形数量  
C. 开发者年龄与项目成立时间  
D. 业务重要性与实现难度或风险  

### 54. 缩短心跳间隔可加快故障发现，但会提高消息量，并在网络抖动时增加误切换概率。心跳间隔在此主要表现为

A. 已经证明的非风险点  
B. 权衡点  
C. 只影响单一质量属性且无其他影响的参数  
D. 业务功能本身  

### 55. 团队希望比较多个改进方案的预期质量收益与实施成本，并支持投资优先级决策。相较SAAM和ATAM，最直接对应

A. CBAM  
B. DFD  
C. RMS  
D. 2PL  

### 56. 某可修复服务平均无故障工作时间为999小时，平均修复时间为1小时。按题设稳态模型，其可用度为

A. 99%  
B. 99.99%  
C. 99.9%  
D. 90%  

### 57. 两个独立组件在给定任务时段内可靠度均为0.9。将它们组成任一组件正常即可工作的主动并联系统，假定切换/汇合机制完全可靠，其可靠度为

A. 0.81  
B. 0.99  
C. 0.90  
D. 1.00  

### 58. 订单已提交成功，但调用方超时后重试导致重复扣款。系统采用“至少一次”消息投递，最应增加

A. 让每次重试都生成一个全新业务订单号  
B. 只扩大消息队列容量  
C. 取消消息确认并无限重试  
D. 业务幂等键与原子去重/业务更新机制  

### 59. 某慢服务故障导致上游线程长期占满，需要在失败达到阈值后快速拒绝调用，并在一段时间后少量试探恢复。该机制为

A. 熔断器  
B. 普通轮询负载均衡  
C. 数据库范式分解  
D. 只做请求日志采集  

### 60. 要根据负载自动增加无状态Web服务的Pod副本数量，应主要使用

A. 仅修改每个容器的日志级别  
B. 仅创建一个ConfigMap  
C. Horizontal Pod Autoscaler  
D. 只扩大单个Pod的内存上限  

### 61. 某Pod进程仍存活，但初始化尚未完成，不应接收线上请求。最直接控制它是否作为服务流量后端的探针是

A. Liveness probe  
B. Readiness probe  
C. 只检查容器日志文件是否存在  
D. 仅通过镜像标签判断  

### 62. 按经典CAP讨论，网络分区发生时，若系统坚持线性一致性，则可能需要

A. 在所有分区都无条件返回成功且保持线性一致性  
B. 通过增加缓存完全消除分区  
C. 保证任意两个客户端立即看到所有未提交修改  
D. 拒绝或延迟某些请求，牺牲CAP意义下的可用性  

### 63. 传统SOAP Web Service技术组合中，描述服务接口与消息格式、供客户端生成调用代码的主要规范是

A. WSDL  
B. ARP  
C. RAID  
D. RMS  

### 64. 老系统必须持续服务，团队在入口按业务路径逐步把流量转向新实现，并在迁移完成后下线对应旧模块。这体现

A. 一次性整体停机替换  
B. 仅代码格式化而无行为变化  
C. 绞杀者式渐进迁移  
D. 禁止新旧系统并存的迁移方式  

### 65. 某实时分析方案同时维护历史数据批处理层、实时处理层以及面向查询的服务层，典型地属于

A. 单一流处理路径的Kappa架构  
B. Lambda架构  
C. 传统两层C/S架构  
D. 只提供接口适配的适配器模式  

### 66. 流处理任务按事件发生时间统计窗口，事件可能乱序到达。Watermark的主要作用是

A. 保证现实网络中绝不会再到达迟到事件  
B. 给每条数据提供身份认证证书  
C. 自动把全部乱序数据改为严格有序传输  
D. 表达事件时间进度，帮助判断何时推进窗口计算  

### 67. 单处理器抢占式实时调度中，周期任务独立、截止期等于周期，忽略开销。RMS分配固定优先级的主要依据是

A. 周期越短，优先级越高  
B. 本次绝对截止时间越晚，优先级越高  
C. 已等待时间越短，优先级越高  
D. 代码文件越大，优先级越高  

### 68. 项目活动A、B可同时开始，工期分别为3天、4天；C依赖A，工期5天；D依赖A和B，工期2天；E依赖C和D，工期3天。最短项目工期为

A. 9天  
B. 10天  
C. 11天  
D. 12天  

### 69. 某检查点PV=100万元、EV=80万元、AC=90万元。按挣值分析，项目状态为

A. 进度提前、成本节约  
B. 进度落后、成本超支  
C. 进度落后、成本节约  
D. 进度提前、成本超支  

### 70. 按照我国《计算机软件保护条例》，关于软件著作权保护范围，正确的是

A. 软件登记前不产生任何著作权  
B. 只要算法思想相同，任何独立实现都必然侵犯软件著作权  
C. 源程序可以受保护，与之有关的文档一律不能受保护  
D. 可以保护符合条件的程序和有关文档，但不延及所用思想、处理过程、操作方法或数学概念本身  

## 第71—75题共用材料

A platform is split into services aligned with business capabilities. Each service should hide internal details behind a stable (71). To handle redelivery, a consumer uses a business key so that processing the same operation again does not create an additional effect; this property is called (72). When a dependency repeatedly fails, a (73) can temporarily reject calls and later probe recovery. Multiple service instances across independent failure domains improve (74), while a trace identifier helps engineers follow a request across service boundaries and supports (75).

### 71. 根据英文材料，空（71）应填

A. interface  
B. interrupt  
C. instruction  
D. inheritance  

### 72. 根据英文材料，空（72）应填

A. latency  
B. volatility  
C. idempotency  
D. fragmentation  

### 73. 根据英文材料，空（73）应填

A. garbage collector  
B. circuit breaker  
C. page table  
D. syntax parser  

### 74. 根据英文材料，空（74）应填

A. coupling  
B. redundant billing  
C. lock contention  
D. availability  

### 75. 根据英文材料，空（75）应填

A. observability  
B. normalization  
C. compilation  
D. pagination  

本卷归档用于历史去重、答题复盘与迁移复测。答案及解析不在试卷正文中公布。
